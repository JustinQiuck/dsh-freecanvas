---
title: Pending Tests
description: Implemented changes that still need manual verification
---

# Pending Tests

The current release needs manual verification in these areas:

- DSH clean-install automation: GitHub Actions run `32737054081` passed the full clean-runner lifecycle against pinned `@deepseek-ai/dsh@0.1.1-rc.2`: build, 29 host/contract tests, candidate-package verification, disposable-profile install and reinstall, bundled page/static asset/disabled-channel checks, uninstall, and base Web restart. The automated profile intentionally disables Canvas Agent and does not read or write the user's `~/.infinite-canvas`.
- DSH Desktop lifecycle acceptance: final candidate SHA256 `2e3ff2db70e99584674930d1e36151ed29007b64cb8660266f568a0fa60e649f` passed an isolated-profile lifecycle on DSH Desktop `2.0.1` with bundled DSH `0.1.0-rc.7`. First boot, full cold restart, the unique sidebar entry, conversation/split/canvas layout restoration, packaged Canvas Agent health, and one read-only MCP inventory/call all passed (`34` tools). A non-empty canvas and a synthetic non-secret provider configuration both survived the final candidate's changed Desktop origin; the fixed-candidate sequence also verified canvas retention across a same-version reinstall. Both remained visible with no current-origin browser warnings/errors and were stored with `0700` directory and `0600` item permissions. The test canvas and configuration were deleted, uninstall removed only FreeCanvas and its child processes, the plugin marketplace remained available, and the daily `desktop` profile was restored. The official channel stayed disabled and the inspected logs contained no KIE endpoint, bearer credential, device token, or official-account file.
- DSH release-package contract: `npm --prefix plugins/dsh-freecanvas run verify:package` should perform only a dry-run and must neither publish to npm nor leave a `.tgz`. The current bundle manifest, host/client entry points, bundled Web app, plugin-specific Changelog, ELv2, and upstream notices should pass. Removing a required file, pointing the patch at another package, dropping the current DSH `0.1.1` settings compatibility line, or placing a synthetic credential fixture in candidate text should fail clearly without echoing the full fixture.
- Official AI channel capabilities: a fresh configuration should expose only `gpt-image-2` for images and `grok-imagine-video` for videos. Text and audio should require separately configured channels; without them, reverse prompting, text nodes, audio nodes, and plugin text generation must open configuration instead of falling back to an image model. Adding a text or audio channel should restore the corresponding model picker and generation flow.
- Official channel UI: Settings must render exactly one official image/video channel before custom providers, without edit or delete controls and without an API-key field. Selecting it while disconnected must open the account drawer from Image Studio, Video Studio, canvas generation, and plugin nodes; a missing text/audio model must still open normal provider settings. In an enabled isolated test profile, the drawer must support one-time pairing through a validated account-portal URL, flag an expired credential, show only public points/account state, accept a masked card code, render a purchase link only for HTTPS URLs, clear submitted codes, and confirm disconnect. A lost mutation response must refresh public state before suggesting a retry, so a card is not blindly redeemed twice. The compact points entry must not cover canvas-top-bar controls at narrow widths. Keep the host toggle off outside that test profile.
- Official media channel configuration gate: plugin settings should default to `officialChannelEnabled=false` and `officialChannelSingleUserMode=false`, so entering URLs alone cannot enable the feature. Future use requires explicit single-user local mode plus valid New API and account-portal URLs; production accepts only HTTPS URLs without credentials, queries, or fragments. Development mode may accept HTTP only for `127.0.0.1`, `localhost`, or `::1`; all other HTTP URLs and malformed URLs must remain rejected. This version must not issue official-channel requests or save/display a KIE key or device token.
- Official host proxy: in an explicitly enabled local test profile, `/dsh-freecanvas/official-api/status` must reveal only the enablement, connection state, public wallet URL, and expiry. Pair exchange must save the 30-day device token only in the host's `~/.infinite-canvas/official-account.json` file with `0700`/`0600` permissions and never return it to the browser, configuration export, WebDAV, support package, or logs. Check that malformed or cross-site requests, arbitrary paths/queries/models, remote DSH access, HTTP production upstreams, oversized bodies, and forwarded browser credentials fail closed. Confirm fixed image/video routes stream safely; a failed remote disconnect must isolate the local credential instead of allowing more calls. Run `npm --prefix plugins/dsh-freecanvas run test:host` in an isolated profile before any deployment; keep the official toggle off.
- New API FreeCanvas pairing: a real dashboard browser session should receive a 5-minute one-time pairing code; a personal access token must be rejected. The Wallet “连接 DSH FreeCanvas” card must generate only after a click, render countdown/copy/loading/failure/expired/regenerate states, disclose that image/video prompts and reference media are sent to KIE, and never restore a code after refresh. Exchanging the code once should create a 30-day `freecanvas_device` token restricted to `gpt-image-2,grok-imagine-video`; expired, replayed, malformed, disabled-user, deleted-user, and concurrent exchanges must fail without creating another token. Redis and database reads must retain the internal purpose. The generic Token API must neither export nor modify the device token, while an ordinary token request must not be able to set its purpose. Pairing and exchange responses must use `Cache-Control: no-store` and must not echo a pairing code in an error response. This requires a backed-up New API test deployment; the FreeCanvas official-channel toggle must remain off.
- New API FreeCanvas wallet: only a `freecanvas_device` token should read the account or redeem a 32-character card code. The account must return decimal-string `points` and `used_points` derived solely from `User.Quota`/`UsedQuota`, a validated HTTPS purchase-card link, and the current token expiry—never a raw token, KIE credit, key, or unrelated profile field. A valid card must increase the authoritative quota exactly once; invalid, reused, expired, disabled, and malformed cards must share a safe error. `DELETE /api/freecanvas/device` must revoke only the requester’s device and remain safe to repeat. The existing generic redemption log must not contain the full card code. This still requires the same backed-up New API test deployment and must not enable the FreeCanvas official channel.
- DSH bundle installation: `npm pack` should build and include the canvas frontend under `web/`; installing that artifact into an isolated or Desktop profile should add one `ui-dsh-freecanvas` loader entry, open the bundled canvas without Docker, Vite, or a port 3000 service, preserve conversation/split/canvas layout preferences and nested routes, load icons and local canvas plugins under `/dsh-freecanvas/`, and start the packaged Canvas Agent dependency. Setting `canvasUrl` explicitly should switch the same-origin route to external proxy mode. Removing the package should remove its profile bundle entry without changing unrelated plugins.
- DSH canvas layout entry: after a full Desktop restart, plugin loading should complete without a persistent loading screen or renderer CPU loop, and the previously closed/open canvas state plus its Split or Canvas layout should be restored. Opening the bundled canvas should start the Canvas Agent through Electron's Node mode, make its health endpoint available, and allow DSH canvas MCP calls without `fetch failed`. The always-visible conversation/split/canvas control should no longer cover the canvas header. The FreeCanvas sidebar row should expose a trailing current-layout button whose menu selects Conversation, Split, or Canvas; Conversation should hide the canvas, Split and Canvas should open it, and the main FreeCanvas button should reopen the last visible canvas layout. The menu should close on outside click, scrolling, window resize, or Escape and support arrow, Home, and End key navigation.
- DSH user-facing identity cleanup: the Agent connection panel and startup/update logs should explain that DSH Desktop manages the bundled local Agent connection automatically and should no longer show upstream package names or plugin/MCP installation and removal commands. Manual Local URL and Connect token fields must remain available for connection troubleshooting. New configuration exports should download as `dsh-freecanvas-config.json`, and a fresh WebDAV setup should default to the `dsh-freecanvas` directory while existing internal storage and compatibility identifiers remain unchanged.
- DSH bundle licensing: package version `0.2.0` should declare `Elastic-2.0`; the packed artifact should contain the complete Elastic License 2.0, component scope, and upstream MIT notices, while repository-level documentation should no longer claim that every component uses MIT. Historical plugin version `0.1.0` and upstream MIT rights must remain clearly documented, and no current document should claim that unreleased Pro features are already available.
- Repository and product identity: README, docs, web UI, Canvas Agent, and the Codex marketplace should show DSH FreeCanvas; README and docs should use the DSH plugin marketplace as the user entry, state that the canvas and Agent ship with the plugin, and no longer present standalone startup, Vercel, Render, or Docker deployment guides; the new FreeCanvas logo and project capability cards should replace inherited visual assets and remote demo images. Legacy contact, QQ, sponsor, promotional, and source live-demo navigation should remain absent, while source-development links, source attribution, the active `@basketikun/canvas-agent` dependency, compatibility identifiers, and MIT copyright remain intact.
- Canvas navigation: the canvas should open in Move mode by default, and the bottom toolbar should switch between Select and Move modes and reflect the active mode with its icon; Select should draw a clearly spaced dashed range whose dash length, gap, and stroke width remain visually constant at every zoom level, while Move should drag elements directly and pan only when dragging the blank background. Clicking blank canvas space in either mode should deselect the current node and close its top toolbar and lower prompt panel. Holding Control or Space should temporarily invert the active tool without changing the toolbar mode: Select becomes Move, and Move becomes range selection. After clicking any canvas-page button, pressing Space should only invert the tool and must not trigger that button again. Middle-button panning from any position and additive node selection with Shift/Cmd should continue to work, while Space remains available inside text inputs and editable content.
- English and Simplified Chinese switching across navigation, settings, titles, descriptions, Ant Design components, and persisted preferences.
- Global Dropdown, Menu, Select, Cascader, and TreeSelect popup backgrounds, hover states, and selected states in both light and dark themes.
- Canvas node resize stability: importing an image should open its prompt panel, resizing it immediately from any corner should temporarily hide the panel, remain smooth without flashing or triggering a maximum update depth error, and restore the panel after release. Prompt scrolling, generated-prompt restoration, edit-prompt persistence after closing an existing image or text node panel, expanded prompt editing with live synchronization and `@` references, image editing, drag-and-drop references, and generation configuration should continue to work.
- Text output count: both a text node's lower prompt panel and a generation configuration node in Text mode should expose an independent generation count from 1 to 15, default to one, and create that many text nodes. Changing the text count must not alter the image count, and switching a generation configuration node back to Image should continue to use the configured image count.
- Node editing entry points: the image toolbar should default to icons without labels and no longer include Edit; the text toolbar should no longer include Edit or Edit text. Clicking an image, text, or video node without dragging should open its lower prompt panel directly, while image toolbar labels can still be enabled explicitly from the toolbar settings.
- Canvas side panel: clicking an element row should smoothly center and select its node without zooming above 100%; image elements with content should still open the large preview without triggering canvas focus.
- Multi-image canvas generation: starting N images should immediately create one image node with N expandable slots and one incoming connection; each empty slot should independently show generating or failure while requests are running, every slot with image content should always display that image, and the first successful image should appear as the primary image immediately. The collapsed state should use a compact stack with at most three closely spaced backplates and a clearly legible count control; only clicking the count control should expand or collapse the slots upward and to the right, while double-clicking the image should open its large preview. Every completed secondary image should keep a clearly legible primary-image action visible in both themes. Expanded groups should remain expanded when clicking elsewhere, dragging, resizing, or previewing, and multiple groups may remain expanded together.
- Multi-image primary selection: choosing another primary image should preserve the node center and current maximum edge while adapting to the new image ratio; free-resize nodes should keep their exact dimensions, and the viewport zoom indicator must not change.
- Failed multi-image slots: every failed slot, including the current primary slot, should expose retry and delete actions. Retry should update only that slot without replacing an existing successful primary image; delete should remove only that failed slot and update the displayed count.
- Multi-image resize: resizing the root node while child images are expanded should keep the group expanded and resize normally without triggering nested React updates or an application error.
- Multi-image alternatives: expanded images should use up to four columns so larger batches stay compact; double-clicking any completed child image should preview that exact image without changing the primary image. Every completed image, including the primary image, should provide an independent download action, while alternatives should also keep clearly legible create-copy and set-primary actions in both themes. Create-copy should add an independent image node to the right of the group, select it, and leave the original image group unchanged.
- Canvas Agent startup, MCP status, model and permission controls, approvals, version reporting, diagnostics, streaming responses, history consistency, multi-tab isolation, and local Skill management.
- Agent header tabs in English and Chinese at different panel widths; labels should collapse to icons and counts before either edge is clipped.
- Agent message layout, Markdown, code blocks, attachments, token statistics, progress timelines, command groups, and scroll-follow behavior.
- Agent inline references: type `/` to select an enabled Skill or `@` to select a canvas asset at the caret; tokens should remain aligned with text, support keyboard navigation and hover previews, and preserve their positions after sending or refreshing.
- Agent message metadata: image attachments, Skills, canvas references, and local previews should survive page refreshes and Agent restarts through exact `clientMessageId`, `threadId`, and `turnId` ownership; protocol upgrades must not reset the independent storage format, unknown or damaged storage must refuse writes without overwriting files, no records may be silently pruned by count or size, and deleting a thread should remove its metadata and preview assets without relying on browser IndexedDB or text matching.
- Prompt Center layout, source synchronization, search, asset insertion, prompt detail dialogs, and compact split-layout prompt pickers in the image and video workbenches.
- Workbench history image cleanup: after generating content in the image or video workbench, deleting an asset, canvas image node, canvas content, or canvas project must not remove thumbnails, results, or references from generation history; deleting a generation record should still remove its dedicated local files.
- Settings import/export and WebDAV-related configuration.
- Local storage settings: the Settings page should provide a Local storage tab that reads only the `infinite-canvas` database and shows IndexedDB usage, total site usage, browser quota, quota percentage, and per-object-store record counts and content size; refreshing after local data changes should update the figures without blocking the rest of the page.

The Chinese version contains the complete item-by-item acceptance checklist.
